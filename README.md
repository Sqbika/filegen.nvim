# filegen.nvim
An extensible plugin that was made due to not wanting to configure another plugin into the shape I need.
So instead I just made this?

# Vision
* Generate project/context accurate files like in your ex-IDE
    * Angular? use `ng-generate` nvim plugin or native cli generation
    * Java? create the usual boilerplate or even more! or less? your choice
    * Not found? Create your own in a .template

# TODO
The rest of readme.
